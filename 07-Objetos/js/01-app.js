/* 
 como se crea un objeto
 ! forma literalmente
 ? primero la propiedades del objetos
*/

const nombre = "Monitor 20 pulgada";
const precio = 300;
const disponible = true;

const producto = {
  nombre: "Monitor 20 pulgada",
  precio: 300,
  disponible: true,
};

console.log(producto);
